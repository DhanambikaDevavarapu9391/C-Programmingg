#include <stdio.h>
int main() {
  int num;
  printf("Enter a number: ");
  scanf("%d", &num);
  int result = cube(num);
  printf("Cube of %d is %d\n", num, result);
  return 0;
}
int cube(int n) {
  return n * n * n;
}
